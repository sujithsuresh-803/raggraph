apiVersion: v1
kind: Service
metadata:
  name: raggraph
  labels: { app: raggraph }
spec:
  type: ClusterIP        # front with an Ingress/LoadBalancer for external access
  selector:
    app: raggraph
  ports:
    - name: http
      port: 80
      targetPort: 8000
